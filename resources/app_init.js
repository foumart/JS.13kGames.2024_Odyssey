// Set debug to true if you want to see logs about PWA caching / fetching of resources and other output.
let _debug;

// Record if the game is being run as a PWA in its own window, separate from the browser.
let _standalone;
