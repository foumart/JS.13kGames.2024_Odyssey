class Unit {
	
	constructor(x, y, type) {
		this.x = x;
		this.y = y;
		this.type = type;

		//this.origin = 0;// flag color
		//this.selection = 0;// only for player (player marked with green contour)
		//this.overlay = 0;// only for player (draws the player underlay object)
	}
	
}
